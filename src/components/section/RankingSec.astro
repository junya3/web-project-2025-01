---
import { Image } from 'astro:assets';
import SecTitle from '/src/components/parts/SecTitle.astro';
import Contents from '/src/data/contentsList.js';
---

<section>
  <SecTitle title="出展ランキング" />
  <ul>
    {
      Contents.content
        .flatMap((array) => array) // ネストされた配列をフラットにする
        .filter((content) => content.ranking) // rankingがある要素だけを抽出
        .sort((a, b) => a.ranking - b.ranking) // ranking順にソート
        .map((content) => (
          <li>
            {content.ranking}位: {content.title}
          </li>
        ))
    }
  </ul>
</section>
